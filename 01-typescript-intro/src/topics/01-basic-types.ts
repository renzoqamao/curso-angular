const name: string = 'renzo';
let hpPoints : number | string | 'FULL' = 95;
const isAlive: boolean = true;
hpPoints= 'FULL';

console.log({
    name, hpPoints, isAlive
})

export {}; // opcional, ya esta por defecto.